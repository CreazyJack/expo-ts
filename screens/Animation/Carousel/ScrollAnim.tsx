import { Text } from 'react-native';
import Animated from 'react-native-reanimated';

export default () => {
  return (
    <Animated.ScrollView>
      <Text>ScrollAnim</Text>
    </Animated.ScrollView>
  );
};
